<?xml version="1.0"?>
<robot xmlns:xacro="http://wiki.ros.org/xacro">

  <!-- Load the real UR arm description macro -->
  <xacro:include filename="$(find ur_description)/urdf/ur_macro.xacro"/>

  <!-- UR5e-specific defaults -->
  <xacro:arg name="joint_limit_params" default="$(find ur_description)/config/ur5e/joint_limits.yaml"/>
  <xacro:arg name="kinematics_params" default="$(find ur_description)/config/ur5e/default_kinematics.yaml"/>
  <xacro:arg name="physical_params" default="$(find ur_description)/config/ur5e/physical_parameters.yaml"/>
  <xacro:arg name="visual_params" default="$(find ur_description)/config/ur5e/visual_parameters.yaml"/>

  <xacro:arg name="safety_limits" default="false"/>
  <xacro:arg name="safety_pos_margin" default="0.15"/>
  <xacro:arg name="safety_k_position" default="20"/>

  <!-- Generic macro expected by robot.urdf.xacro -->
  <xacro:macro name="armada_arm" params="tf_prefix parent *origin">

    <!-- ur_robot requires `name`, but this macro does not use it internally.
     Link/joint names are controlled by tf_prefix. -->
    <xacro:ur_robot
      name=""
      tf_prefix="${tf_prefix}"
      parent="${parent}"
      joint_limits_parameters_file="$(arg joint_limit_params)"
      kinematics_parameters_file="$(arg kinematics_params)"
      physical_parameters_file="$(arg physical_params)"
      visual_parameters_file="$(arg visual_params)"
      safety_limits="$(arg safety_limits)"
      safety_pos_margin="$(arg safety_pos_margin)"
      safety_k_position="$(arg safety_k_position)">

      <xacro:insert_block name="origin"/>

    </xacro:ur_robot>

    <!-- Prototype manipulation TCP, 21 cm along the tool0 +Z axis. -->
    <link name="tcp_link"/>
    <joint name="${tf_prefix}tool0_tcp_joint" type="fixed">
      <parent link="${tf_prefix}tool0"/>
      <child link="tcp_link"/>
      <origin xyz="0 0 0.195" rpy="0 0 0"/>
    </joint>

  </xacro:macro>

</robot>
